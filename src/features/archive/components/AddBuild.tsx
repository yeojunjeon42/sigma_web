import ContactPill from "@/components/ContactPill";

export default function AddBuild() {
  return (
    <ContactPill
      about="archive"
      above
      pill={"Built something in the club? Add it to the archive"}
    />
  );
}
