// The pointer as the field's print reads it: DepthStage writes it each frame, FieldScreen reads it.
export const FIELD_POINTER = { x: -1e4, y: -1e4, k: 0 };
/** Radius, in px, of the patch of print the pointer warms. */
export const WARM = 190;
