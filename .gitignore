# dependencies
/node_modules

# build output
/.next/
/out/
*.tsbuildinfo
next-env.d.ts

# env and local tooling
.env*
.vercel
*.pem
.DS_Store
npm-debug.log*
.playwright-cli/

# AI agent instruction files (the repo is public)
CLAUDE.md
.claude/

# Local working material, not part of the site. docs/ holds the club's private source
# material (member data and faces) and must never be committed.
/docs/
/scripts/
/DESIGN.md
